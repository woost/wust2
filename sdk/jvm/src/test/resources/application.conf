wust.test {
    wustServer {
        host = "localhost"
        port = 8901
        user = "wust"
        email = "test@woost.space"
        password = "wust"
    }
    appServer {
        host = "localhost"
        port = 8080
        webhookPath = "woostHook"
        allowedOrigins = [
            "http://localhost:12345",
            "https://woost.space"
        ]
    }
    oAuth {
        authPath = "oauth/auth"
        clientId = "clientId"
        clientSecret = "topSecret"
        siteUri = "http://localhost"
        authorizeUrl = "/wust/oauth/authorize"
        tokenUrl = "/wust/oauth/token"
    }
}
wust.test2 {
    wustServer {
        host = "localhost"
        port = 8901
        user = "wust"
        email = "test@woost.space"
        password = "wust"
    }
    appServer {
        host = "localhost"
        port = 8080
        webhookPath = "woostHook"
        allowedOrigins = [
            "http://localhost:12345",
            "https://woost.space"
        ]
    }
    oAuth {
        authPath = "oauth/auth"
        clientId = "clientId"
        clientSecret = "topSecret"
        siteUri = "http://localhost"
    }
}
