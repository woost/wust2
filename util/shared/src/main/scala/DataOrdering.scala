package wust.util

/**
  * Algorithms to define an ordering on nodes
  *
  * Default: NodeIds => time based (cuuid)
  */
object DataOrdering {


  //  def getLastInOrder(graph: NestedArrayInt, parentIds: Array[Int]): Int = {
  //
  //  }
  //
//  def apply[T](graph: GraphLookup, elements: List[T], extractNodeIdx: T => Int, extractNodeId: T => NodeId, parentId: NodeId): Edge = {
//
//    graph.childrenIdx(graph.idToIdx(into.nodeId)).toList
//    val orderedNodes = graph.topologicalSortByIdx[Int](elements, extractNodeIdx, identity) // rebuild ordering in container
//    Edge.Before
// }

}
