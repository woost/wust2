server {
    listen 80;

    root /public;

    location ~* \.(css|js|html)$ {
        gzip_static on;
        gzip_types application/javascript text/css;
    }

    location = / {
        index index.html;
    }
}
