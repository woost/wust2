wust.slack {
    accessToken = ${WUST_SLACK_TOKEN}
    wust {
        host = ${WUST_HOSTNAME}
        port = ${WUST_BACKEND_PORT}
        user = ${WUST_USER}
        password = ${WUST_PASSWORD}
    }
}
