wust.gitter {
    accessToken = ${WUST_GITTER_TOKEN}
    wust {
        host = ${WUST_HOSTNAME}
        port = ${WUST_BACKEND_PORT}
        user = ${WUST_USER}
        password = ${WUST_PASSWORD}
    }
}
